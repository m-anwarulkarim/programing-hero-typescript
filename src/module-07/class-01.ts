// 🌟 Class & Object in TypeScript : 1.Object 2.Class 3.constructor 4.Method 5.Parameter Properties
// ===============================================

/*
---------------------------------------------------
🏷️ 1. Object কী?
---------------------------------------------------

Object হলো key–value আকারে ডেটা রাখার একটি গঠন (Structure)।

👉 Object এর ভিতরে থাকে:
   • Properties (key + value)
   • Methods (function)

✔️ উদাহরণ:
const user = {
  name: "Anwar",
  age: 22,
  isAdmin: false,
  greet() {
    return `Hello, ${this.name}`;
  },
};

👉 এখানে:
- name, age, isAdmin  → Property
- greet()             → Method
*/

// ******************************************************

/*
---------------------------------------------------
🏛️ 2. Class কী?
---------------------------------------------------

Class হলো Object তৈরির জন্য একটি Blueprint বা Template।

➡ একই ধরনের স্ট্রাকচারের একাধিক object তৈরি করতে Class ব্যবহার করা হয়।

✔️ সাধারণ একটি উদাহরণ:
class User {
  name: string;
  age: number;

  constructor(name: string, age: number) {
    this.name = name;
    this.age = age;
  }

  greet() {
    return `Hello ${this.name}`;
  }
}

👉 object তৈরি:
const userA = new User("Anwar", 22);
const userB = new User("Karim", 25);

console.log(userA.greet());
*/

// ******************************************************
// 🐾 বাস্তব উদাহরণ: Animal Class
// ******************************************************

class Animal {
  name: string;
  specieh: string;
  sound: string;

  constructor(name: string, specieh: string, sound: string) {
    /*
    constructor এর কাজ:
    ---------------------
    - Class এর property গুলোকে মান দেওয়া
    - আমরা object তৈরি করার সময় যে value পাঠাই, তা receive করা
    */
    this.name = name;
    this.specieh = specieh;
    this.sound = sound;
  }

  // 👉 Method তৈরি
  makeSound() {
    console.log(`The animal ${this.name} is making sound`);
  }
}

/*
---------------------------------------------------
📌 গুরুত্বপূর্ণ পয়েন্ট:
---------------------------------------------------

✔ method এর ভিতরে name বা sound আলাদা করে গ্রহণ করতে হয় না  
  → কারণ এগুলো Class-এর property, তাই সরাসরি this.name এবং this.sound দিয়ে ব্যবহার করা যায়।

✔ class নিজেই একটি টাইপ হিসেবে কাজ করে  
  → আলাদা type বা interface ব্যবহার না করলেও হয়।  
    উদাহরণ: const dog: Animal = new Animal(...)

✔ constructor হলো বিশেষ method  
  → object তৈরি হওয়ার সাথে সাথেই এটি স্বয়ংক্রিয়ভাবে কল হয়।

✔ class হলো reusable blueprint  
  → একই স্ট্রাকচারের যত খুশি object তৈরি করা যায়, শুধু মান ভিন্ন হবে।
*/

// ******************************************************
// 🐶 Object তৈরি (from Animal class)
// ******************************************************

const dog = new Animal("Tommy", "Dog", "Bark");

console.log(dog.name); // Property access
dog.makeSound(); // Method call

// ******************************************************
// 🧩 Parameter Properties ব্যবহার করলে class আরও ছোট ও clean হয়
// ******************************************************

class Animal2 {
  constructor(
    public name: string,
    public specieh: string,
    public sound: string
  ) {}
  makeSound() {
    console.log(`The animal ${this.name} is making sound`);
  }
}

/*
Parameter Properties:
----------------------
✔ constructor এর ভিতরে property declare + assign — দুটোই এক লাইনে হয়ে যায়  
✔ আলাদা করে this.name = name; লিখতে হয় না  
*/

// ******************************************************
// 📊 Object vs Class (সহজ তুলনা)
// ******************************************************

/*
| বিষয়                | Object                           | Class                               |
| -------------------- | --------------------------------- | ------------------------------------ |
| সংজ্ঞা               | Key–value আকারে ডেটা            | Object বানানোর Blueprint            |
| তৈরি হয়              | সরাসরি literal দিয়ে              | new keyword দিয়ে                     |
| পুনরায় ব্যবহারযোগ্য   | তুলনামূলক কম                     | খুব সহজ                              |
| Method থাকে?          | থাকে                              | থাকে                                 |
| Type Checking         | type/interface লাগতে পারে        | class নিজেই টাইপ হিসেবে কাজ করে      |
*/
